packages:
  - "apps/*"
  - "services/*"
  - "packages/*"

# Keep install artifacts local; these directories are already ignored by Git.
storeDir: .pnpm-store
cacheDir: .cache/pnpm
stateDir: .cache/pnpm-state

# Native compiler used by the NestJS Vitest transform.
allowBuilds:
  "@swc/core": true
